apiVersion: v1
kind: Service
metadata:
  name: concertdb-service
spec:
  selector:
    app: concertdb
  ports:
    - protocol: TCP
      port: {{ (index .Values.concertdb.ports 0).port }}
      targetPort: {{ (index .Values.concertdb.ports 0).targetPort }}
  type: ClusterIP